#include <iostream>
#include <ctime>
#include <cstdlib>
using namespace std;

int main() {
    const int m = 3, n = 4;
    int a[m][n];
    int choice;

    cout << "Choose mode:\n";
    cout << "1 - Numbers 0..9 and show all max indexes\n";
    cout << "2 - Numbers without limits (rand) and show max index\n";
    cout << "Enter choice: ";
    cin >> choice;

    srand(time(0));

    int maxValue;

    switch (choice) {

    case 1: {
        maxValue = 0;
        cout << "\n--- MODE 1: Values 0..9 ---\n\n";

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                a[i][j] = rand() % 10;
                cout << a[i][j] << "\t";

                if (a[i][j] > maxValue)
                    maxValue = a[i][j];
            }
            cout << "\n";
        }

        cout << "\nMax value = " << maxValue << "\n";
        cout << "Indexes:\n";

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (a[i][j] == maxValue)
                    cout << "(" << i + 1 << ", " << j + 1 << ")\n";
            }
        }

        break;
    }

    case 2: {
        maxValue = INT_MIN;
        cout << "\n--- MODE 2: Unlimited numbers (rand) ---\n\n";

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                a[i][j] = rand();
                cout << a[i][j] << "\t";

                if (a[i][j] > maxValue)
                    maxValue = a[i][j];
            }
            cout << "\n";
        }

        cout << "\nMax value = " << maxValue << "\n";
        cout << "Index:\n";

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (a[i][j] == maxValue)
                    cout << "(" << i + 1 << ", " << j + 1 << ")\n";
            }
        }

        break;
    }

    default:
        cout << "Invalid choice!\n";
        break;
    }

    return 0;
}
